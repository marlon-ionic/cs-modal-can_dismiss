import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-modal-can_dismiss',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
